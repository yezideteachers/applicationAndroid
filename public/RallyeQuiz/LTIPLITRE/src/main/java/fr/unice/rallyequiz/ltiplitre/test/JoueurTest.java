package fr.unice.rallyequiz.ltiplitre.test;

import android.content.Context;
import android.location.*;
import android.test.ActivityInstrumentationTestCase2;
import android.os.Bundle;
import android.test.InstrumentationTestCase;
import android.util.Log;
import android.widget.EditText;


import fr.unice.rallyequiz.ltiplitre.R;

/**
 * Created by root on 09/04/14.
 */
public class JoueurTest extends InstrumentationTestCase {

    /*public void verifiePosition() throws Exception {
        LocationManager locationManager = (LocationManager) Context.getSystemService(Context.LOCATION_SERVICE);
    }
       postion afficher sous format de Text

        EditText position;
        position.equals((Edit))

       Location networkLocation = locationManager.getLastKnownLocation(LocationManager.NETWORK_PROVIDER)
       LocationListener LocationListener = (LocationListener)

        LocationListener.onLocationChanged(Location);
    }


    /*
    tester le positionement du joueur
     */
}
